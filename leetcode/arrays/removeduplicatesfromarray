class Solution {
    public int removeDuplicates(int[] nums) {
        
        int uniqueNum = 0;

        for(int currentNum : nums){

            if(uniqueNum == 0 || currentNum != nums[uniqueNum -1]){

                nums[uniqueNum++]=currentNum;
            }
        }
        return uniqueNum;

    }
}
